#include "LayerTester.cpp"


RocketLayerTester::RocketLayerTester(RocketLayer &layer,
				     std::string inputFile,
				     std::string outputFile,
				     int (*checkPyros)(void*, SensorData),
				     void* pyroCheckerContext,
				     Messages * messages,
				     int numMessages) {
  
}

RocketLayerTester::runSimulations(int numSimulations) {
  
}

RocketLayerTester::runSimulation() {
  RocketLayer
}
