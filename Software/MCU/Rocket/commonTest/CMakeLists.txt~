cmake_minimum_required(VERSION 3.16)

project("common_test")

set("common_test_sources")
set("common_test_include")

add_library("${PROJECT_NAME}" ${common_test_sources})
target_include_directory("${PROJECT_NAME}" PUBLIC "${common_test_include}")

